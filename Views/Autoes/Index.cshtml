@model IEnumerable<AutoDijelovi1.Models.Auto>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NazivAuta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GodinaProizvodnje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Skladiste)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NazivAuta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GodinaProizvodnje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Skladiste.SkladisteId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AutoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AutoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AutoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
